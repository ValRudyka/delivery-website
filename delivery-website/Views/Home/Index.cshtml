@{
    @using Microsoft.AspNetCore.Identity
    ViewData["Title"] = SharedLocalizer["Home.Title"];
    @inject SignInManager<IdentityUser> SignInManager
    @inject UserManager<IdentityUser> UserManager
}

<!-- Hero Section -->
<div class="hero-section">
    <div class="container text-center">
        <h1>🍕 @SharedLocalizer["Home.HeroTitle"]</h1>
        <p>@SharedLocalizer["Home.HeroSubtitle"]</p>
        <a asp-area="Customer" asp-controller="Restaurants" asp-action="Index" class="btn btn-light btn-lg">
            @SharedLocalizer["Home.ViewRestaurants"]
        </a>
    </div>
</div>

<div class="container">
    <!-- Admin Panel Quick Access (Only for Admin Users) -->
    @if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
    {
        <div class="alert alert-danger border-danger shadow-sm my-4" role="alert">
            <div class="d-flex align-items-center justify-content-between">
                <div>
                    <h5 class="alert-heading mb-1">
                        <i class="bi bi-shield-lock-fill me-2"></i>@SharedLocalizer["Home.AdminAccess"]
                    </h5>
                    <p class="mb-0">@SharedLocalizer["Home.AdminAccessText"]</p>
                </div>
                <a asp-area="Admin" asp-controller="Dashboard" asp-action="Index" class="btn btn-danger">
                    🛡️ @SharedLocalizer["Home.OpenAdminPanel"]
                </a>
            </div>
        </div>
    }

    <!-- Main Features -->
    <div class="section-title text-center mb-4">
        <h2>🌟 @SharedLocalizer["Home.WhyChooseUs"]</h2>
        <p class="text-muted">@SharedLocalizer["Home.WhyChooseUsSubtitle"]</p>
    </div>

    <div class="row mb-5">
        <div class="col-md-4 mb-4">
            <div class="card text-center p-4 h-100">
                <div style="font-size: 3.5rem;">🍔</div>
                <h5 class="mt-3">@SharedLocalizer["Home.WideSelection"]</h5>
                <p class="text-muted">@SharedLocalizer["Home.WideSelectionText"]</p>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="card text-center p-4 h-100">
                <div style="font-size: 3.5rem;">🚀</div>
                <h5 class="mt-3">@SharedLocalizer["Home.FastDelivery"]</h5>
                <p class="text-muted">@SharedLocalizer["Home.FastDeliveryText"]</p>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="card text-center p-4 h-100">
                <div style="font-size: 3.5rem;">💳</div>
                <h5 class="mt-3">@SharedLocalizer["Home.EasyPayment"]</h5>
                <p class="text-muted">@SharedLocalizer["Home.EasyPaymentText"]</p>
            </div>
        </div>
    </div>

    <!-- How It Works -->
    <div class="section-title text-center mb-4">
        <h2>📱 @SharedLocalizer["Home.HowItWorks"]</h2>
        <p class="text-muted">@SharedLocalizer["Home.HowItWorksSubtitle"]</p>
    </div>

    <div class="row mb-5">
        <div class="col-md-3 mb-3">
            <div class="card text-center p-3">
                <div class="bg-primary text-white rounded-circle mx-auto mb-3" style="width: 50px; height: 50px; line-height: 50px; font-size: 1.5rem;">
                    1
                </div>
                <h6>@SharedLocalizer["Home.Step1Title"]</h6>
                <small class="text-muted">@SharedLocalizer["Home.Step1Text"]</small>
            </div>
        </div>
        <div class="col-md-3 mb-3">
            <div class="card text-center p-3">
                <div class="bg-primary text-white rounded-circle mx-auto mb-3" style="width: 50px; height: 50px; line-height: 50px; font-size: 1.5rem;">
                    2
                </div>
                <h6>@SharedLocalizer["Home.Step2Title"]</h6>
                <small class="text-muted">@SharedLocalizer["Home.Step2Text"]</small>
            </div>
        </div>
        <div class="col-md-3 mb-3">
            <div class="card text-center p-3">
                <div class="bg-primary text-white rounded-circle mx-auto mb-3" style="width: 50px; height: 50px; line-height: 50px; font-size: 1.5rem;">
                    3
                </div>
                <h6>@SharedLocalizer["Home.Step3Title"]</h6>
                <small class="text-muted">@SharedLocalizer["Home.Step3Text"]</small>
            </div>
        </div>
        <div class="col-md-3 mb-3">
            <div class="card text-center p-3">
                <div class="bg-primary text-white rounded-circle mx-auto mb-3" style="width: 50px; height: 50px; line-height: 50px; font-size: 1.5rem;">
                    4
                </div>
                <h6>@SharedLocalizer["Home.Step4Title"]</h6>
                <small class="text-muted">@SharedLocalizer["Home.Step4Text"]</small>
            </div>
        </div>
    </div>

    <!-- Popular Cuisines -->
    <div class="section-title text-center mb-4">
        <h2>🍽️ @SharedLocalizer["Home.PopularCuisines"]</h2>
        <p class="text-muted">@SharedLocalizer["Home.PopularCuisinesSubtitle"]</p>
    </div>

    <div class="row mb-5">
        <div class="col-6 col-md-3 mb-3">
            <div class="card text-center p-3">
                <div style="font-size: 2.5rem;">🍕</div>
                <h6 class="mt-2">@SharedLocalizer["Home.Italian"]</h6>
                <small class="text-muted">@SharedLocalizer["Home.ItalianText"]</small>
            </div>
        </div>
        <div class="col-6 col-md-3 mb-3">
            <div class="card text-center p-3">
                <div style="font-size: 2.5rem;">🍣</div>
                <h6 class="mt-2">@SharedLocalizer["Home.Japanese"]</h6>
                <small class="text-muted">@SharedLocalizer["Home.JapaneseText"]</small>
            </div>
        </div>
        <div class="col-6 col-md-3 mb-3">
            <div class="card text-center p-3">
                <div style="font-size: 2.5rem;">🍔</div>
                <h6 class="mt-2">@SharedLocalizer["Home.American"]</h6>
                <small class="text-muted">@SharedLocalizer["Home.AmericanText"]</small>
            </div>
        </div>
        <div class="col-6 col-md-3 mb-3">
            <div class="card text-center p-3">
                <div style="font-size: 2.5rem;">🥟</div>
                <h6 class="mt-2">@SharedLocalizer["Home.Ukrainian"]</h6>
                <small class="text-muted">@SharedLocalizer["Home.UkrainianText"]</small>
            </div>
        </div>
    </div>

    <!-- Stats Section -->
    <div class="card bg-light mb-5">
        <div class="card-body">
            <div class="row text-center">
                <div class="col-md-3 mb-3">
                    <h3 class="text-primary">500+</h3>
                    <p class="text-muted mb-0">@SharedLocalizer["Home.Restaurants"]</p>
                </div>
                <div class="col-md-3 mb-3">
                    <h3 class="text-primary">10,000+</h3>
                    <p class="text-muted mb-0">@SharedLocalizer["Home.ActiveUsers"]</p>
                </div>
                <div class="col-md-3 mb-3">
                    <h3 class="text-primary">50,000+</h3>
                    <p class="text-muted mb-0">@SharedLocalizer["Home.OrdersPerMonth"]</p>
                </div>
                <div class="col-md-3 mb-3">
                    <h3 class="text-primary">4.8⭐</h3>
                    <p class="text-muted mb-0">@SharedLocalizer["Home.AverageRating"]</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Call to Action -->
    <div class="card bg-primary text-white text-center mb-5">
        <div class="card-body p-5">
            <h3 class="mb-3">@SharedLocalizer["Home.ReadyToOrder"]</h3>
            <p class="mb-4">@SharedLocalizer["Home.JoinThousands"]</p>
            @if (!SignInManager.IsSignedIn(User))
            {
                <a asp-area="Customer" asp-controller="Restaurants" asp-action="Index" class="btn btn-light btn-lg me-2">
                    @SharedLocalizer["Home.ViewRestaurants"]
                </a>
                <a asp-controller="Account" asp-action="Register" class="btn btn-outline-light btn-lg">
                    @SharedLocalizer["Account.RegisterButton"]
                </a>
            } else
            {
                <a asp-area="Customer" asp-controller="Restaurants" asp-action="Index" class="btn btn-light btn-lg">
                    @SharedLocalizer["Home.ViewRestaurants"]
                </a>
            }
        </div>
    </div>
</div>
